workflows:
  ios-build:
    name: iOS Build (No Signing)
    environment:
      flutter: stable
      xcode: latest
    scripts:
      - flutter pub get
      - flutter build ios --release --simulator
    artifacts:
      - build/ios/iphonesimulator/*.app
